version: '3.5'

services:
  max2tg:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      LOG_DIR: /app/logs
    volumes:
      - ./logs:/app/logs
    ulimits:
      nproc: 65535
      nofile:
        soft: 65535
        hard: 65535
